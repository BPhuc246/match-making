# events {}

# http {

#     client_max_body_size 10M;
#     resolver 127.0.0.11 valid=10s;

#     server {
#         listen 80;
#         server_name matchmakinggame.duckdns.org;

#         # Required for Let's Encrypt validation
#         location /.well-known/acme-challenge/ {
#             root /var/www/certbot;
#         }

#         # Redirect everything else to HTTPS once cert exists
#         location / {
#             return 301 https://$host$request_uri;
#         }
#     }

#     server {
#         listen 443 ssl;
#         server_name matchmakinggame.duckdns.org;

#         ssl_certificate /etc/letsencrypt/live/matchmakinggame.duckdns.org/fullchain.pem;
#         ssl_certificate_key /etc/letsencrypt/live/matchmakinggame.duckdns.org/privkey.pem;

#         # React frontend
#         location / {
#             proxy_pass http://frontend:80;

#             proxy_http_version 1.1;

#             proxy_set_header Host $host;
#             proxy_set_header X-Real-IP $remote_addr;
#             proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#             proxy_set_header X-Forwarded-Proto $scheme;
#         }

#         # REST API
#         location /api/ {
#             proxy_pass http://backend:8080;

#             proxy_http_version 1.1;

#             proxy_set_header Host $host;
#             proxy_set_header X-Real-IP $remote_addr;
#             proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#             proxy_set_header X-Forwarded-Proto $scheme;

#             proxy_connect_timeout 5s;
#             proxy_read_timeout 300s;
#             proxy_send_timeout 300s;
#         }

#         # WebSocket
#         location /api/ws/ {
#             proxy_pass http://backend:8080;

#             proxy_http_version 1.1;

#             proxy_set_header Upgrade $http_upgrade;
#             proxy_set_header Connection "upgrade";

#             proxy_set_header Host $host;
#             proxy_set_header X-Real-IP $remote_addr;
#             proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#             proxy_set_header X-Forwarded-Proto $scheme;

#             proxy_read_timeout 60s;
#             proxy_send_timeout 60s;
#         }
        
#         location = /nginx-health {
#             return 200 "ok";
#             add_header Content-Type text/plain;
#         }
        
#     }
# }

# First-time cert generation
# events {}
# http {
#     server {
#         listen 80;
#         server_name tododevops.duckdns.org;

#         location /.well-known/acme-challenge/ {
#             root /var/www/certbot;
#         }

#         location / {
#             return 200 "ok";
#         }
#     }
# }

events {}

http {
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;

    server {
        listen 80;
        server_name matchmakinggame.duckdns.org;

        location /.well-known/acme-challenge/ {
            root /var/www/certbot;
        }

        location / {
            return 301 https://$host$request_uri;
        }
    }

    server {
        listen 443 ssl;
        server_name matchmakinggame.duckdns.org;

        ssl_certificate     /etc/letsencrypt/live/matchmakinggame.duckdns.org/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/matchmakinggame.duckdns.org/privkey.pem;

        root /usr/share/nginx/html;
        index index.html;

        location /api/ {
            proxy_pass http://backend:8080;
            proxy_http_version 1.1;

            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;

            proxy_connect_timeout 5s;
            proxy_send_timeout 60s;
            proxy_read_timeout 60s;
        }

        location /api/ws/ {
            proxy_pass http://backend:8080;
            proxy_http_version 1.1;

            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";

            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;

            proxy_read_timeout 60s;
            proxy_send_timeout 60s;
        }

        location / {
            try_files $uri $uri/ /index.html;
        }
    }
}